# Correction

Ce dossier contient les corrections du Jour 1 Sandbox.

## Helm

La correction du sujet Helm se trouve dans :

- [helm-campus-sandbox](/C:/Users/h4mdi/Desktop/sandbox/training/labs/jour-1-sandbox/correction/helm-campus-sandbox)

Cette chart reprend la version attendue du lab :

- un template `Deployment` backend ;
- un template `Service` backend ;
- un template `Deployment` frontend ;
- un template `Service` frontend ;
- un template `Route` OpenShift ;
- un `values.yaml` centralisant les valeurs.
